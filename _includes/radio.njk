{% extends "base.njk" %}

{% block content %}
<div class="page-content">
  <a class="home-link" href="/">← back to home</a>

  <h1>{{ title }}</h1>

  {{ description | safe }}

  {% if audio %}
    <p><a href="{{ audio }}" target="_blank" rel="noopener">Listen to radio session</a></p>
  {% endif %}
</div>
{% endblock %}