{% extends "base.njk" %}

{% block content %}
<div class="page-content">
  <a class="home-link" href="/">← back to home</a>

  <h1>{{ title }}</h1>

  {{ description | safe }}

  {% if video %}
    <p><a href="{{ video }}" target="_blank" rel="noopener">Watch video</a></p>
  {% endif %}
</div>
{% endblock %}